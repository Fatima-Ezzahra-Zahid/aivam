<div class="last-card">
    <span>{{ "Newsletter"|t }}</span>
    <p>{{ "Inscrivez-vous à notre newsletter"|t }}</p>{#TODO#}
    <form {{ attributes }}>
        {{ title_prefix }}
        {{ children }}
        {{ title_suffix }}
    </form>
</div>